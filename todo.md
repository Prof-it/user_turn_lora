- [x] journal/ local not commited with lab / change journal for agent to work better an keep track myself
- [x] notebook/ folder for legacy notebook then

create everything as .py files modular etc as runpod and lamdba failed with jupyter noteebok sow e have modular which would be best for ablation anyway and then could dockerize potentially and deploy on GPUs on gcloud or via docker to runpod etc

more compute GPUS would also speed up training etc which currently with whole noteebok takes 1.5h so no fats iteration etc

everything documented and results saved so when writing or checking results etc we dont run into the issue of havng to rerun
which will cost time

use tueplots for plotting as it provides like templates for conferences also etc
https://github.com/pnkraemer/tueplots
https://tueplots.readthedocs.io/en/latest/

as the notebooks code is verified make sure when cretaing .py integration and modules that the produce the same results
